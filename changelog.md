# Changelog

本文件记录墨读各版本的用户可见变化。

## 维护规则

- 本文件采用 append-only 方式维护。
- 新版本只能追加在文件末尾，版本按发布时间正序排列。
- 已发布版本的内容不得修改、删除或重新排序。
- 每个版本应记录版本号、发布日期以及 Added、Changed、Fixed、Performance、Security 中适用的分类。
- 没有内容的分类不需要保留。

## 0.1.0 — 2026-08-25

### Added

- 首个 macOS 原生只读 Markdown 阅读器版本。
- 支持通过系统目录选择器打开本地目录。
- 左侧目录树默认读取一层，并按需展开子目录。
- 目录树展示所有文件，Markdown 文件可在主区域打开阅读。
- Markdown 文档自动生成右侧文章大纲，并支持点击跳转。
- 支持标题、段落、列表、任务列表、引用、代码、表格、链接和本地图片。
- 内置纸页、极简、暖沙和墨夜四套阅读主题。
- 支持构建 macOS APP 和 DMG 安装包。

### Security

- 应用采用只读目录权限，不修改源文件。
- 文档完全在本地解析，不上传内容。
- 远程图片默认不加载。
- 阻止读取当前工作目录之外的路径。

### Performance

- 使用 SwiftUI 和 AppKit 原生组件，不捆绑 Chromium、Node 或 WebView 运行时。
- 目录树采用懒加载，折叠目录后释放对应子树。
- 切换文档时取消旧读取和渲染任务。
- 本地图片按照阅读尺寸降采样。

## 0.2.0 — 2026-08-25

### Changed

- 重做 Markdown 正文排版，中文正文使用宋体，标题使用苹方。
- 将正文阅读宽度调整为约 700 pt，并重新设计字号、行距和段距。
- 调整四套阅读主题的背景、正文、标题、辅助文字和强调色对比度。
- 重新设计引用、任务列表、行内代码、代码块和表格的视觉层次。
- 缩减文档信息栏和大纲栏顶部高度，为正文释放更多空间。

### Fixed

- 修复复杂 Markdown 文档首次打开后可能停留在文章中段的问题。
- 修复多行代码块背景不完整、行距异常的问题。
- 修复 Markdown 链接回调对阅读视图形成强引用的问题。

### Performance

- 移除高内存成本的原生文本块和表格组件实现。
- 使用轻量富文本属性完成代码块、引用和表格排版。
- 复杂语法样稿调试运行时物理内存约 64 MB，系统检查结果为 0 leaks。

## 0.3.0 — 2026-08-25

### Added

- 新增 GitHub、Newsprint、Minimal、Sepia 和 Dark 五套完整 Markdown 渲染主题。
- 支持工作目录内 Markdown 文档链接跳转，并保留目标标题锚点。
- 支持通过安全本地资源协议显示工作目录内的图片。

### Changed

- 将正文渲染从 TextKit 富文本切换为 macOS 系统 WKWebView 中的语义化 HTML 与 CSS。
- 表格改用真实的 table、th 和 td 结构，单元格可独立换行和对齐。
- 代码块改用 pre 和 code 结构，并按主题统一代码容器、语言标签和行内代码样式。
- 标题大纲改用稳定 HTML 锚点跳转，切换文档后默认回到文章顶部。

### Fixed

- 修复长文本和多个行内代码出现在表格中时无法按单元格正确排版的问题。
- 修复代码块背景依赖补空格导致的边界不完整和换行生硬问题。
- 移除链接及链接内代码的额外背景，只保留主题强调色和下划线。

### Security

- WebKit 使用非持久化数据存储，并通过内容安全策略禁止页面网络连接、远程字体、远程图片、媒体、对象和内嵌页面。
- 原始 HTML 以转义文本显示，不允许文档注入可执行标签。
- 本地图片必须位于已选择目录内、使用允许的图片格式且不超过 40 MB。
- 页面导航由应用拦截；远程链接仅在用户点击后交给系统打开。

### Performance

- 不再为整篇文档创建大型属性字符串或 TextKit 表格对象，正文只保留生成后的 HTML。
- 61 KB、162 个标题的复杂样稿在 Release 模式稳定运行约 30 秒后，应用与三个系统 WebKit 辅助进程的 RSS 合计约 137 MB。
- 系统检查结果为应用主进程、GPU 和网络进程 0 leaks；WebContent 进程存在 144 字节的系统字体注册表残留。

## 0.3.1 — 2026-08-25

### Changed

- 右侧大纲改为原生可拖拽分栏，支持在 240–480 pt 范围内调整宽度。
- 增大大纲标题和条目字号，并强化当前条目的色条与背景标识。

### Fixed

- 修复左侧目录标题栏的刷新按钮错误打开目录选择器的问题；现在会在保留现有只读授权的同时重新扫描目录并重载当前文档。
- 修复正文滚动位置与大纲当前项不联动的问题；正文滚动、标题跳转和大纲点击现在会保持一致。
- 修复长文档的当前大纲项移出可见区域后无法自动跟随的问题。

## 0.3.2 — 2026-08-25

### Fixed

- 修复正式沙盒 `.app` 双击启动后，目录和大纲正常但 Markdown 正文区域全白的问题。

### Changed

- 构建脚本在应用签名后自动执行 WKWebView 正式沙盒渲染冒烟检查，检查失败时中止打包。

### Security

- 为 macOS 沙盒 WKWebView 内容进程补充系统要求的出站客户端能力。
- 文档页面仍通过内容安全策略禁止网络连接、远程资源、媒体和内嵌页面；应用自身不新增网络请求。

## 0.4.0 — 2026-08-26

### Added

- 文件树支持 Return 就地重命名、Command-C 复制绝对路径，并提供对应右键菜单。
- 支持将第二篇 Markdown 文档在参考栏打开；两栏独立保留文档、大纲当前项和滚动跳转状态，右侧大纲跟随当前活动栏。
- 新增最近打开目录，使用安全作用域书签最多记录 8 条，并在启动时自动恢复最后一次目录。

### Changed

- 将软件浅色/深色外观与 Markdown 正文渲染样式拆分为两个独立设置。
- 左侧刷新仅重新加载文件列表，右上角刷新仅重新读取当前文档并重建对应大纲。
- 目录授权由只读调整为读写，写权限当前只用于用户主动执行的重命名操作。

### Fixed

- 允许渲染 Markdown 中的 HTTP/HTTPS 远程图片，并为图片请求启用延迟加载和 `no-referrer`。
- 文件列表忽略 `.DS_Store` 和 `._` 开头的 AppleDouble 元数据，同时继续显示 `.env`、`.github` 等普通点文件和点目录。
- 增大右侧大纲字号，并保留 240–480 pt 的可拖拽宽度。

### Security

- 远程资源权限仅开放给图片；页面脚本、Fetch/XHR/WebSocket、远程字体、媒体、对象和内嵌页面仍由内容安全策略禁止。
- 本地图片继续限制在当前授权目录、允许的图片格式和 40 MB 大小以内。

## 0.5.1 — 2026-08-26

### Changed

- 优化 Markdown 文件顶部 YAML metadata 的展示：改用紧凑、无框线的 key/value 信息区；内容较多时默认展示前四项，并可展开或收起完整内容。

## 0.5.0 — 2026-08-26

### Added

- 新增应用内离线 Mermaid 渲染，固定使用 Mermaid 11.16.1；图表可突破窄正文栏，并为加载、失败和源代码回退提供独立状态。
- 保留侧栏顶部原有目录卡片样式；点击后通过显示目录名与完整路径的独立面板切换最近使用的目录。
- 修复 `.agents/skills` 等内部目录符号链接只能展开、无法列出 `SKILL.md` 的问题；子项继续显示为逻辑路径。
- 文件树背景只表示当前树焦点或重命名状态，不再把阅读窗中已经打开的文件长期标注为选中。
- 文件树右键菜单显示回车键、Command-C 和 Option-回车快捷键提示。
- 支持直接预览 `.html` 与 `.htm` 静态页面，并从 HTML 标题生成联动大纲；页面脚本和主动连接保持禁用。
- 文件树支持上下方向键移动，右方向键展开目录或进入子项，左方向键收起目录或返回父级。
- 重新设计应用图标，以深墨底、展开纸页和连续墨痕构成更清晰的小尺寸阅读标识。

### Changed

- 两个阅读栏改为完全平级，默认均分并支持拖动中间分界线调整宽度；点击任一阅读栏会将其设为活动栏，文件树随后在该栏打开所选文档。
- 将软件外观与 Markdown 样式重新合并为纸页、极简、暖沙、墨夜和 GitHub 五套统一主题；每套主题均支持跟随系统、明墨和暗墨。
- 宽表格改为突破窄正文栏、优先利用阅读区域宽度并保留可读列宽；空间不足时只在表格容器内横向滚动，最后一列不再被挤成逐字换行。
- 右侧大纲可在 260–560 pt 范围内拖动调整宽度，并进一步增大条目字号。
- 移除窗口工具栏中重复的动态文档标题，文件名只在对应阅读栏显示一次。
- 目录重新扫描时保持文件树视图与键盘焦点状态，方向键长按可连续移动。

### Fixed

- 修复文件树当前项可能出现额外深色焦点边框的问题，仅保留加重背景。
- 修复跨行选择正文时，自定义选择样式把无文字区域连成整行色块的问题。
- 修复 `.agents/skills` 等指向工作目录内部的符号链接目录被误判为普通文件、无法展开的问题。
- 修复双栏场景下文件树始终替换固定阅读栏、而不是当前活动栏的问题。
- 修复重复选择当前最近目录会清空已打开文档和双栏状态的问题。
- 修复 WebKit 正文进程被系统回收后可能留下空白阅读区的问题，视图会自动恢复当前文档与位置。
- 修复目录快速收起再展开时旧读取任务仍可能回写结果的问题；每个目录节点只保留一个可取消加载任务。
- 后台目录读取、文件读取和 Markdown/HTML 渲染统一传递取消状态，并在重操作前检查取消，避免快速切换时旧任务及大字符串积压。
- 工作区访问权限改由任务共享的生命周期会话管理；切换目录后，旧权限只在相关读取真正结束时释放。
- WebContent 进程每份文档最多自动恢复一次，连续失败时停止重载并显示手动刷新提示，避免崩溃循环。
- 修复打开或关闭第二阅读栏、隐藏后重新显示大纲时，已拖拽的大纲宽度被原生分栏重新布局覆盖的问题；宽度现在会跨文档、主题和窗口重绘持久保留。

### Security

- Mermaid JavaScript 随应用打包，不从 CDN 动态下载；页面仅对这一应用内资源协议开放脚本权限。
- Mermaid 使用 strict 安全级别，限制输入大小和边数量；单张图失败不会影响其余正文与图表。

## 0.6.0 — 2026-08-26

### Added

- 新增简体中文与英文界面，默认跟随 macOS 的系统或单独应用语言设置；菜单、工具提示、错误状态、主题名称和文档渲染状态均使用同一套本地化资源。
- 新增常驻工具栏分栏入口与 `⌘\` 快捷键：单栏时打开并聚焦空的第二阅读栏，双栏时关闭当前阅读栏；左右两栏保持平级。

### Changed

- 国际交付名称统一为 `MoDu`：应用包、DMG 文件和挂载卷分别使用 `MoDu.app`、`MoDu-0.6.0.dmg` 和 `MoDu`；中文环境中的应用显示名称继续使用“墨读”。

## 0.6.1 — 2026-08-26

### Changed

- HTML 修正为直接运行原始页面，保留 CSS、JavaScript、动画、键盘和鼠标交互，并支持加载左侧已授权工作区内的同级、子目录及上级关联资源。
- HTML 不再套用 Markdown 主题或默认转换为静态正文；页面声明的 HTTPS 图片、样式、字体、脚本、媒体和内嵌内容可按 WebKit 网页规则加载。

### Fixed

- 修复依赖脚本激活当前页的 HTML 演示文稿只显示深色背景的问题；此类页面现在按原始交互逻辑启动，原文件加载失败时才降级为禁用脚本的静态长页。
- 修复左侧目录刷新时先清空并重建整棵树造成的闪烁和位置跳变；刷新现在按路径增量合并，复用未变化节点，并保持已展开目录与键盘焦点。
- 修复左侧目录与右侧大纲分别被固定限制为 360 pt 和 560 pt、在大窗口中无法继续拖宽的问题；两侧最大宽度现在随当前窗口动态调整为约三分之一，并继续为正文保留最小可用宽度。
- 修复右侧大纲分割条使用自身局部坐标导致拖动宽度只变化约一半的问题；现在按全局鼠标位移一比一调整，并使用稳定的系统光标跟踪区域显示左右调整指针。
- 修复首次启动时普通操作按钮自动获得键盘焦点并显示选中框的问题；侧栏、工具栏、文档栏、大纲及欢迎页的按钮和菜单统一退出 Tab 焦点链，仅保留文件树键盘操作、重命名输入框和正文交互所需的真实输入焦点。

### Security

- HTML 的本地读取权限限制在用户从左侧打开并授权的工作区；越界文件导航会被拒绝，网页数据使用非持久化会话。
- 用户点击的顶层 HTTP/HTTPS 链接继续交给系统浏览器，脚本发起的顶层远程跳转不会替换当前阅读区域；HTML 作者脚本本身可以发起网络请求，应仅交互运行可信来源的原型。

## 0.7.0 — 2026-08-27

### Added

- 新增常见代码、配置和纯文本文件直接预览，覆盖 Swift、C/C++、Objective-C、Java/Kotlin、JavaScript/TypeScript、Python、Go、Rust、Shell、JSON、YAML、TOML、Properties、Dockerfile 等常见类型。
- 新增无后缀文本文件预览；优先识别特殊文件名和 shebang，无法推断语言时以纯文本展示。
- 新增离线语法高亮，固定使用应用内 Highlight.js 11.12.0；高亮语言按文件扩展名或特殊文件名确定，也可在源码栏中为单个文件手动调整或切换为纯文本。
- 新增全阅读器通用的文档内查找，覆盖 Markdown、HTML、代码、配置和纯文本；支持区分大小写、上一个与下一个匹配项及 Command-F，源码还支持跨分段扫描和回绕。
- Markdown 与 HTML 中指向工作区内代码、配置或纯文本文件的链接可以直接进入源码预览。
- 新增常见图片文件直接预览，支持 PNG、JPEG、GIF、WebP、HEIC、TIFF、BMP、ICO、AVIF 和 SVG，并在阅读区域内保持比例居中适配。
- 文件树中按空格键可在当前活动阅读栏打开键盘焦点所在的文件；回车键继续用于就地重命名。
- 新增源码行号跳转：使用 Command-L 显示唯一的行号输入框，回车确认跳转、Esc 取消，输入框失去焦点时自动关闭；无效或越界行号会保留输入框并标记错误。

### Changed

- 源码与行号背景铺满整个阅读区域，不再使用带外边距和圆角的卡片容器。
- 查找栏默认隐藏，Command-F 显示并聚焦、Esc 关闭；源码语言选择放在文件标题栏右侧。
- 源码分段采用无感连续滚动：接近当前内容顶部或底部时自动加载相邻分段，不显示上一段、下一段按钮或分段行号范围。
- 源码高亮按关键字、类型、方法、字符串、字面量、元数据和变量分别使用更易区分的配色，减少类型密集代码中大面积近似蓝色的问题。

### Fixed

- 修复右侧大纲左边界的宽度调整热区显示成独立框体的问题；现在恢复为原有的单一边框线，同时保留可拖拽宽度。
- 修复点击打开目录中的文件时，所选条目被反复动画定位到目录中央而造成的列表抖动；仅键盘导航需要时才滚动条目到可见范围。
- 修复从强制暗墨或明墨切换为跟随系统时，正文可能短暂沿用旧配色且需要重新载入才恢复的问题。
- 文档主题与明暗模式菜单明确标记当前已应用选项。

### Performance

- 源码文件不设置总大小上限；读取按最多约 256 KiB 或 2000 行分段，只缓存当前分段及相邻分段。
- 文件内检索逐段读取并响应取消，不需要先把整个文件解码为字符串或构建完整 WebKit 页面。
- WebView 同时最多保留三个相邻源码分段；追加、前插或移除远端分段时补偿滚动偏移，在维持连续阅读的同时限制正文 DOM 规模。
- 行号跳转按需扫描分段定位目标行，不需要预先加载或解码整个文件。

### Security

- 源码内容进入页面前统一进行 HTML 转义，文件中的标签和脚本不会执行。
- 源码页面仅允许加载精确白名单中的应用内高亮脚本，禁止图片、网络连接、远程资源、媒体、对象、内嵌页面和 Worker。
- Highlight.js 版本、上游提交、许可证和文件摘要随应用源码记录，不在运行时访问 CDN。

## 0.7.1 — 2026-08-27

### Fixed

- 修复超长单行源码在同一帧同时触发向前、向后分段加载时其中一个方向请求被丢弃，导致连续滚动永久卡住的问题。
- 修复无后缀目录或管道被误判为纯文本源码，并在打开后进入读取失败页的问题。
- 修复裸 CR 换行不受每段 2000 行限制，以及 CRLF 跨越字节分页边界时被拆成两个换行、造成空行和行号偏移的问题。
- 修复查找文本跨越源码分页边界时错误报告未找到的问题；向前与向后查找均会读取查询所需的最小后继重叠内容。
- 修复切换到不支持预览的文件后仍持有上一份源码会话，以及切换高亮语言时正在执行的查找状态无法结束的问题。
- 修复 WKWebView 尚未完成文档和源码高亮加载时立即查找，可能在不完整页面上错误报告无匹配且加载完成后不再重试的问题。
- 修复损坏或内容格式不符的图片仅凭扩展名进入已加载状态的问题；图片现在会先通过 ImageIO 或安全 XML 解析验证实际内容。
- 修复重命名目录后，其子文件手动设置的语法高亮语言覆盖丢失并残留旧路径记录的问题。
- 修复 WebView 多阶段冒烟检查共用启动时总超时，导致后续阶段在自身截止时间前被旧计时器误判失败的问题。

### Performance

- 源码页位置记录改为每 256 页一个稀疏检查点，并只保留最近 512 条位置；扫描超大短行文件时不再为每个分页永久增长索引。
- 源码连续滚动按方向独立管理加载任务，并通过最多四条更新的有界队列完整交付同一刷新周期内的双向分页结果。

## 0.7.2 — 2026-08-27

### Changed

- 阅读栏的文档、源码会话、搜索、行号跳转和异步任务统一收口到单栏状态，关闭或迁移分栏时以同一生命周期整体取消或恢复。
- Markdown、HTML、源码和图片改用类型化渲染内容，渲染模式与其专属数据不能再组合成无效状态。
- 本地文档协议、图片格式、MIME 类型、资源大小和离线脚本版本改由统一策略维护，资源协议处理器不再由视图层承载。
- 新增 SwiftPM 测试目标，完整确定性自检只在 Debug 与测试阶段编译；交付构建把测试、警告即错误编译、第三方资源校验和中英文 WebKit 冒烟作为统一门禁。

### Fixed

- 修复关闭主阅读栏时仅迁移部分参考栏状态，可能丢失源码可见分页、搜索状态或遗留异步任务的问题。
- 修复源码分页、高亮切换、查找和行号跳转错误覆盖整篇已加载文档的问题；非加载失败现在保留原文档并记录具体操作与恢复方式。
- 修复源码分页失败后 WebView 永久保留该方向 pending 状态的问题；失败方向会被显式释放，错误横幅可直接重试原操作。
- 修复工作区内符号链接完成校验后仍反复通过原始逻辑路径读取、可能在链接被重定向时读到不同目标的问题；读取会话改为保存并复核已验证的规范路径。
- 修复文档预览类型推断隐式读取文件系统的问题；无后缀文件分类现在由调用方显式提供常规文件元数据。
- 修复 Markdown 或 HTML 在大小预检后继续增长时可能绕过完整渲染上限的问题；16 MiB 限制现在直接在文件句柄读取边界执行。
- 修复交互 HTML 在生成大纲后由 WebView 二次读取原文件，可能显示不同版本或绕过 16 MiB 门禁的问题；交互页与静态回退现在共同拥有同一份受限快照。
- 修复 HTML 相对路径图片、脚本和样式直接走 `file:` 读取而绕过资源门禁的问题；本地网页资源统一经工作区协议按通用 MIME 提供，并在句柄边界限制为 40 MiB。
- 修复切换语言、查找或行号跳转取消相邻分页任务后，WebView 仍可能保留未完成方向并停止继续滚动的问题。
- 保留早期 `0.5.1` 与 `0.5.0` 的实际追加顺序，并以本条勘误说明其版本号并非按数值顺序记录，避免重写已交付历史。

### Performance

- Markdown 与 HTML 在 16 MiB 内继续使用完整结构化渲染；超过阈值后自动降级为无总大小上限的连续分页源码视图，避免同时保留完整输入、解析树、回退 HTML 和 WebKit DOM。

### Security

- 新增第三方组件总清单、固定依赖修订、随包许可证和离线 JavaScript 摘要校验；交付构建会拒绝来源记录或摘要不一致的资源。
- 第三方门禁同时校验依赖锁与组件清单双向一致、许可证标识，以及内置组件与受摘要保护资源的归属关系。
- SwiftPM 依赖会在第三方校验前按锁文件物化，并复核实际 checkout 的 HEAD、clean 状态和依赖指纹；随包许可证改由组件清单逐项驱动复制。
- 正式交付默认拒绝 dirty 工作树；显式开发态构建会持久化完整二进制 patch、未跟踪文件归档及其摘要，不再只保留不可逆的源码状态哈希。
- 构建开始、编译完成和 WebKit 冒烟结束都会复核源码状态指纹；构建过程中发生任何输入漂移都会拒绝生成追溯清单和交付物。
- 版本门禁以固定的上一正式交付提交为基线，复核其真实 plist，并要求版本号和构建号严格递增，不再与当前 HEAD 自比较。
- 每次应用与 DMG 交付生成包含版本、构建号、Git 提交、依赖锁摘要、工具链及产物摘要的追溯清单。

## 0.7.3 — 2026-09-01

### Fixed

- 修复打开 HTML 或 HTM 文件时，WebView 的初次页面装载被误判为再次打开当前文档，导致页面无限刷新的问题。
- 修复 HTML 使用 `iframe srcdoc` 承载正文时，内部 `about:srcdoc` 子框架导航被拦截而显示白页的问题。
- HTML 初次装载现在由明确的导航状态识别；工作区内其他文档跳转与外部顶层导航仍沿用原有安全策略。

## 0.8.0 — 2026-09-02

### Added

- 新增 `modu` 命令行启动器；支持用 `modu .` 或目录路径打开工作区，也支持用文件路径打开其所在工作区并立即阅读该文件。
- 命令行入口支持在 MoDu 尚未启动或已经运行时交付新的工作区，并提供 `--help` 与 `--version`。
- 新增带“通用”和“命令行”二级分类的原生设置窗口；集中管理应用语言、明暗模式与命令行工具。语言支持跟随系统、English 和简体中文，并可将 `modu` 一键安装到 `/usr/local/bin/modu` 或安全卸载。

### Changed

- 仓库内的持续开发验证应用改名为 `MoDu Preview.app`（中文显示“墨读预览版”）并使用独立 Bundle Identifier；正式 DMG 内的 `MoDu.app` 名称与身份保持不变。
- 明暗模式从阅读工具栏和“阅读”菜单迁入设置，避免阅读操作与应用级偏好混杂。
- 侧栏底部的功能说明改为品牌文案：中文“字里行间，自有答案”，英文“Answers live between the lines.”。

### Security

- 命令行路径通过 macOS LaunchServices 作为用户打开请求交付；打开单个文件时同时交付其父目录授权，不扩大应用沙盒权限，并继续使用安全作用域书签保存最近工作区。

## 0.8.1 — 2026-09-02

### Fixed

- 修复正式沙盒应用直接请求管理员权限时认证始终失败、导致无法安装或卸载 `modu` 命令的问题。
- 设置页现在会在系统授权期间显示等待状态，并在授权助手退出后自动刷新实际安装状态。
- 修复打开设置窗口时“安装”或“卸载”按钮被系统自动设为初始焦点的问题；按钮仍可通过键盘导航正常聚焦和操作。
- 系统授权窗口会按安装或卸载操作显示明确的中英文权限说明，不再使用缺少具体原因的默认提示。

### Security

- 安装与卸载改由两个职责固定、非驻留的授权助手发起 macOS 系统认证；主应用继续保持沙盒隔离。
- 授权助手不再接收沙盒调用方的命令参数；操作由各自应用包固定声明，目标固定为 `/usr/local/bin/modu`，且卸载只删除仍指向当前应用内置启动器的符号链接。
- 构建门禁新增嵌套助手签名、无沙盒权限和主应用沙盒权限校验。

## 0.8.2 — 2026-09-02

### Fixed

- 恢复「新建窗口」菜单和 `⌘N` 快捷键，确保手动创建的阅读窗口继续使用独立文档状态。
- 修复打开多个 MoDu 窗口时，任一窗口切换文档会同步改动其他窗口预览区和大纲的问题。
- 每个窗口现在独立维护工作区、文件树、文档、双栏阅读、搜索、滚动和大纲状态；语言、外观、阅读主题及最近工作区仍作为应用级偏好共享。
- 菜单命令只操作当前聚焦的阅读窗口；Finder 交付的路径在当前窗口打开，每次 `modu /path` 则创建一个独立阅读窗口，并将同一次的目录与文件交付合并到该窗口。

## 0.8.3 — 2026-09-03

### Changed

- 文件树改为单击选择、双击打开；目录展开箭头仍可单击操作，键盘继续使用空格打开选中文件。
- 双击已经在任一阅读栏打开的文件时，只激活对应阅读栏，不重新加载文档或重置滚动位置。

### Fixed

- 文件树失去键盘焦点后保留中性的非活动选中样式；再次单击条目或目录空白区域即可恢复活动状态，并继续使用方向键操作。
- 复制其他文件路径时不再需要切换右侧文档；选中文件后可直接使用 `⌘C` 复制绝对路径。
- 修复选中文件或目录后使用方向键、空格等文件树快捷键时，可能反复触发 macOS 系统提示音的问题。
- 切换工作区时会清除旧目录选择，并在目录加载后默认选择首个条目；通过命令行直接打开文件时则定位到该文件。
- 文件树只在快捷键对应的操作实际可执行时接管按键，正确操作不再触发系统提示音，无效操作仍保留 macOS 默认响应。

## 0.8.4 — 2026-09-03

### Fixed

- 修复显示区域已选中文本时，`Command-C` 仍被文件树拦截并复制文件路径的问题；文件树快捷键现在只在文件树拥有键盘焦点时生效。
